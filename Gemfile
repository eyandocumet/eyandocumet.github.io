source "https://rubygems.org"

# This gem matches the environment GitHub uses to build your site
gem "github-pages", group: :jekyll_plugins

# Windows sometimes needs this specific server library
gem "webrick"